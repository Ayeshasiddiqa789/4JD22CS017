# Deployment

Site published to GitHub Pages:

- URL: https://Ayeshasiddiqa789.github.io/4JD22CS017/

Steps performed:

1. Initialized local git repository (already present).
2. Created `gh-pages` branch and pushed to `origin`:

```
git checkout -b gh-pages
git push -u origin gh-pages
```

Notes for future updates:

- To update the live site, commit changes to the `gh-pages` branch and push.
- Alternatively, configure GitHub Actions to build from `main` and publish to `gh-pages`.
